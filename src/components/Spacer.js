const Spacer =({height=0, width=0})=>{
    let spacerStyle={
        height: height,
            width: width
    }
    
    return(
        <div style={spacerStyle}>
        </div>
    )
}

export default Spacer